import type Bot from '../bot';

/* ---------------------------------- Meta ---------------------------------- */
export const once = true;
/* -------------------------------------------------------------------------- */

/* --------------------------------- Execute -------------------------------- */
export default (client: Bot) => {
  client.logger.success('Bot is online.');
};
/* -------------------------------------------------------------------------- */
